/* ============================================================
   APP — Bootstrap / DOMContentLoaded entry point
   ============================================================ */

document.addEventListener("DOMContentLoaded", () => {
  applyBrand();
  initParticles();
  initNav();
  initReveal();
  initReleases();
  initCompat();
  renderGallery();
  initPetting();
  initPeeker();
  initPeekerEyes();
});
