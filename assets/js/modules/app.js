/* ============================================================
   APP — Bootstrap / DOMContentLoaded entry point
   ============================================================ */

document.addEventListener("DOMContentLoaded", () => {
  applyBrand();
  initParticles();
  initNav();
  initReveal();
  initCompat();
  renderGallery();
  initPetting();
  initPeeker();
  initPeekerEyes();
});
